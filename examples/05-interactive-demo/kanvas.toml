name = "interactive-demo"
version = "1.0.0"
author = "Kanvas"
description = "Interactive demo combining multiple Phase 1 & 2 features"
mainClass = "Main"

classpath = []
dependencies = []

[modules]
srcDirs = ["src"]
outputDir = "build/classes"

[compiler]
target = "21"
encoding = "UTF-8"

[packaging]
jarName = "interactive-demo.jar"
version = "0.1.0"
nativeTargets = ["windows", "macos", "linux"]
